spring:
  profiles: prod
  datasource:
    url: jdbc:h2:mem:proddb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
app:
  message: "Hello from PROD"
  activeMessage: "PROD active"
